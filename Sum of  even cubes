#include<stdio.h>
#include<math.h>
int main()
{
	int i,n,sum=0;
	printf("sum of cube of even numbers:");
	scanf("%d",&n);
	for(i=0;i<=n;i++)
	{
		if(i%2==0)
		{
			sum+=i*i*i;
		}
	}
	printf("%d",sum);
	return 0;
}

#include<stdio.h>
#include<math.h>
int main()
{
	int i,n,sum=0;
	printf("sum of cube of odd numbers:");
	scanf("%d",&n);
	for(i=0;i<=n;i++)
	{
		if(i%2==1)
		{
			sum+=i*i*i;
		}
	}
	printf("%d",sum);
	return 0;
}
